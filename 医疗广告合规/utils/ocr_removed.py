"""
归档：OCR 模块（已移除）

说明：项目已从主线移除图片识别与 OCR 功能。
此文件保留作为归档副本，避免丢失历史实现。不要在运行时导入此模块。
"""

def ocr_from_image(*args, **kwargs):
    raise RuntimeError("已归档的 OCR 模块：项目不再支持 OCR；请仅使用文本输入接口。")


def set_tesseract_cmd(*args, **kwargs):
    raise RuntimeError("已归档的 OCR 模块：此函数不可用。")
